# 正確的業務邏輯說明

## 核心概念

### 1. FIFO庫存是全局的，不屬於特定帳戶

**關鍵理解：**
- 庫存視為一個整體，不區分來源帳戶
- 所有買入的RMB都進入全局庫存池
- 售出時按FIFO原則從全局庫存中扣減

### 2. 帳戶餘額是各帳戶獨立的

**關鍵理解：**
- 每個帳戶的餘額 = 該帳戶的買入 - 該帳戶的售出扣款 + LedgerEntry變動
- 帳戶餘額與FIFO庫存是兩個獨立的概念
- 帳戶餘額總和 ≠ FIFO庫存總和（這是正常的！）

## 業務流程示例

### 買入流程：
```
買入100 RMB → 入帳A帳戶 → 庫存增加100
買入200 RMB → 入帳B帳戶 → 庫存增加200
此時：全局庫存 = 300 RMB（視為一體）
```

### 售出流程：
```
從B帳戶售出150 RMB：
1. 檢查B帳戶餘額是否足夠（必須 ≥ 150）
2. 從B帳戶扣款150 RMB
3. 從全局庫存按FIFO原則扣減150 RMB（不考慮來源帳戶）
```

**重要：**
- 如果B帳戶餘額 < 150，無法售出（即使全局庫存足夠）
- 庫存按FIFO原則扣減，可能扣減的是A帳戶買入的庫存
- 這是正常的業務邏輯！

## 帳戶餘額計算公式

### 正確的計算方式：

```
帳戶餘額 = 該帳戶作為deposit_account的買入 
         - 該帳戶作為rmb_account的售出扣款
         - LedgerEntry扣款
         + LedgerEntry入款
```

### 錯誤的計算方式（之前）：

```
帳戶餘額 = 該帳戶的FIFO庫存 + LedgerEntry變動  ❌
```

**為什麼錯誤？**
- FIFO庫存是全局的，不屬於特定帳戶
- 帳戶A買入的庫存可能被帳戶B的售出扣減
- 所以不能基於FIFO庫存計算帳戶餘額

## 為什麼帳戶餘額總和 ≠ FIFO庫存總和？

### 正常情況下的差異：

**示例：**
```
帳戶A：買入100，售出扣款0，餘額 = 100
帳戶B：買入200，售出扣款150，餘額 = 50
帳戶餘額總和 = 150

全局庫存：買入300，售出150，FIFO庫存 = 150
```

**如果從帳戶B售出，但庫存按FIFO扣減帳戶A的庫存：**
- 帳戶A餘額：100（沒變，因為沒從A扣款）
- 帳戶B餘額：50（扣了150）
- 帳戶餘額總和：150
- FIFO庫存：150

**結果一致！**

**但如果從帳戶A售出，但庫存按FIFO扣減帳戶B的庫存：**
- 帳戶A餘額：0（扣了100）
- 帳戶B餘額：200（沒變，因為沒從B扣款）
- 帳戶餘額總和：200
- FIFO庫存：150

**結果不一致！差異 = 50**

### 差異的原因：

當售出時的扣款帳戶 ≠ 庫存來源帳戶時，就會產生差異。

**這是正常的業務邏輯，不是錯誤！**

## 修復後的邏輯

### 數據修復API（`/api/admin/data-recovery`）：

```python
# RMB帳戶餘額計算
deposit_amount = 該帳戶作為deposit_account的買入總額
sales_amount = 該帳戶作為rmb_account的售出扣款總額
ledger_debits = LedgerEntry扣款
ledger_credits = LedgerEntry入款

new_balance = deposit_amount - sales_amount - ledger_debits + ledger_credits
```

### 售出時的邏輯（保持不變）：

```python
# 1. 檢查扣款帳戶餘額是否足夠
if deduction_account.balance < rmb_amount:
    raise ValueError("餘額不足")

# 2. 從扣款帳戶扣款
deduction_account.balance -= rmb_amount

# 3. 從全局庫存按FIFO原則扣減
# （不考慮庫存來源帳戶）
```

## 總結

1. ✅ **FIFO庫存是全局的**，不屬於特定帳戶
2. ✅ **帳戶餘額是各帳戶獨立的**，基於買入和售出扣款計算
3. ✅ **售出時從指定帳戶扣款**，庫存按FIFO原則扣減
4. ✅ **帳戶餘額總和 ≠ FIFO庫存總和是正常的**，當扣款帳戶 ≠ 庫存來源帳戶時會產生差異

**之前的分析是錯誤的！** 當前的售出扣款邏輯是正確的，問題在於帳戶餘額的計算方式。

