# 部署環境刪除記錄回滾檢查與修復指南

## 📋 概述

本指南說明如何在 Render 部署環境中檢查和修復刪除記錄未正確回滾餘額的問題。

## 🔍 檢查刪除記錄回滾情況

### 在 Render Shell 中執行

1. **登入 Render Dashboard**
   - 進入您的服務頁面
   - 點擊 "Shell" 標籤

2. **運行檢查腳本**
   ```bash
   cd ~/project/src
   python3 check_deletion_rollback.py
   ```

3. **查看結果**
   - 腳本會自動檢測資料庫類型
   - 顯示所有刪除審計日誌
   - 分析哪些記錄未正確回滾餘額
   - 計算需要修復的金額

### 預期輸出

```
================================================================================
檢查刪除記錄回滾情況
================================================================================

[INFO] 使用部署環境資料庫 (PostgreSQL)
連接字串: postgresql+psycopg://...

找到 X 筆刪除審計日誌

買入記錄刪除: X 筆
售出記錄刪除: X 筆

...
```

## 🔧 修復刪除記錄回滾問題

### ⚠️ 重要警告

修復操作會**直接修改部署環境的資料庫**，請務必：
1. 先執行檢查腳本確認問題
2. 備份資料庫（如果可能）
3. 在非業務高峰期執行

### 執行修復

1. **運行修復腳本（需要確認）**
   ```bash
   python3 fix_deletion_rollback.py
   ```
   
   腳本會顯示需要修復的帳戶和金額，並要求確認。

2. **自動修復（無需確認）**
   ```bash
   python3 fix_deletion_rollback.py --auto-fix
   ```

### 修復邏輯

腳本會：
1. 分析所有刪除審計日誌
2. 找出未正確回滾的記錄
3. 計算每個帳戶需要調整的金額
4. 更新帳戶餘額
5. 創建 LedgerEntry 記錄修復操作

## 📊 檢查結果說明

### 狀態標記

- `[OK]` - 正常，無需修復
- `[WARN]` - 警告，可能未正確回滾（沒有餘額變化記錄）
- `[ERROR]` - 錯誤，餘額回滾不正確

### 問題類型

1. **買入記錄刪除未回滾**
   - RMB帳戶應該減少，但沒有減少
   - 台幣帳戶應該增加，但沒有增加

2. **售出記錄刪除未回滾**
   - RMB帳戶應該增加（回補），但沒有增加

## 🛠️ 疑難排解

### 問題1：找不到 DATABASE_URL

**錯誤訊息：**
```
[ERROR] 未設置 DATABASE_URL 環境變數
```

**解決方法：**
- 在 Render 中，DATABASE_URL 通常會自動設置
- 如果未設置，檢查 Render Dashboard 的環境變數設置
- 或手動設置：`export DATABASE_URL="postgresql+psycopg://..."`

### 問題2：無法導入模組

**錯誤訊息：**
```
ImportError: cannot import name 'CashAccount' from 'app'
```

**解決方法：**
- 確保在正確的目錄：`cd ~/project/src`
- 檢查 `app.py` 是否存在
- 確認所有依賴已安裝：`pip install -r requirements.txt`

### 問題3：資料庫連接失敗

**錯誤訊息：**
```
OperationalError: could not connect to server
```

**解決方法：**
- 檢查網路連線
- 確認 DATABASE_URL 正確
- 檢查 Render 資料庫服務狀態

## 📝 範例輸出

### 檢查結果範例

```
================================================================================
問題總結
================================================================================

發現 3 個問題：

帳戶: 帳戶A (ID: 1, RMB)
  當前餘額: 30,000.00
  問題記錄:
    - purchase #999
      沒有餘額變化記錄，假設未回滾
      需要回滾: -1,000.00
  需要調整金額: -1,000.00
  調整後餘額: 29,000.00

================================================================================
修復建議
================================================================================

以下帳戶需要修復餘額：
...
```

## ✅ 驗證修復結果

修復完成後，可以：

1. **再次運行檢查腳本**
   ```bash
   python3 check_deletion_rollback.py
   ```
   應該顯示：`[OK] 沒有發現餘額回滾問題！`

2. **檢查帳戶餘額**
   - 登入系統
   - 查看現金管理頁面
   - 確認帳戶餘額是否正確

3. **檢查 LedgerEntry**
   - 查看流水記錄
   - 確認有修復操作的記錄

## 🔄 本地測試

在本地環境測試腳本：

```bash
# 使用本地資料庫
python check_deletion_rollback.py

# 使用部署環境資料庫（需要設置 DATABASE_URL）
set DATABASE_URL=postgresql+psycopg://...
python check_deletion_rollback.py
```

## 📞 支援

如果遇到問題：
1. 檢查腳本輸出中的錯誤訊息
2. 查看 Render 日誌
3. 確認資料庫連接正常

